[tool.uv.workspace]
members = ["backend"]

[dependency-groups]
dev = [
    "zizmor>=1.26.1",
]

github-actions = [
    "smokeshow >=0.5.0",
]

[tool.typos.files]
extend-exclude = [
    "img/",
    "uv.lock",
    "bun.lock",
    "backend/coverage/",
    "backend/htmlcov/",
    "backend/app/locales/",
    "frontend/dist/",
    "frontend/src/client/",
    "frontend/public/assets/images/",
]

[tool.typos.default]
extend-ignore-re = [
    # GitHub usernames in @mentions
    "@[a-zA-Z0-9](?:-?[a-zA-Z0-9])*",
]

[tool.typos.default.extend-identifiers]
alls = "alls"
